import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AE1_JonathanMoreraApaza',
  webDir: 'www'
};

export default config;
